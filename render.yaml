services:
  - type: web
    name: crop-prediction
    env: python
    region: us-east
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:flask_app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.8
